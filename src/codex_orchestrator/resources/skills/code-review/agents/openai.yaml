interface:
  display_name: "Code Review"
  short_description: "Review changes and verify prioritized findings"
  default_prompt: "Use $code-review to review the current changes and write an independently verified code-review.md report."

policy:
  allow_implicit_invocation: false
